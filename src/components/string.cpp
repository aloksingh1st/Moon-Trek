#include <bits/stdc++.h>
using namespace std;


string reverseString(string s){
    string temp = "";
    string ans = "";
    int n = s.size();

    for(int i=s.size(); i<=0; i--){

    }

    return ans;
}

int main() {
    string s = "three js is beauty";

    for(int i=s.size(); i<=0; i--){
        cout<<"s[i]"<<" "<<endl;
    }
    // string ans = reverseString(s);
    return 0;
}